# htx-test-sample-project

Small sample codebase: a Python calculator module with unit tests.

Supports add, subtract, multiply, divide, and power.

## Run tests

```bash
python -m unittest discover -s tests -p "test_*.py"
```

## Run demo

```bash
python -m sample_app
```
